{
    "build": {
        "builder": "DOCKERFILE"
    },
    "deploy": {
        "startCommand": "sh -c 'node server/server.js & python3 ai/model.py & npm run preview --prefix client -- --host 0.0.0.0 --port 3000'"
    }
}
